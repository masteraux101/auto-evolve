#!/usr/bin/env python

import subprocess

def create_and_push_branch(branch_name, commit_message, files_to_add):
    """Creates a new branch, commits changes, and pushes to origin."""
    # Implementation details here
    pass
